syntax = "proto3";

package org.opencb.cellbase.grpc;
option java_outer_classname = "GeneService";

import "gene.proto";
import "google/protobuf/any.proto";

service GeneService {
    rpc get (Query) returns (stream Gene) {}
}

message Query {
    repeated string query_items = 1;
}
